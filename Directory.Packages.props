<Project>
  <ItemGroup>
    <!-- Marten + Wolverine -->
    <PackageVersion Include="Marten" Version="9.22.5" />
    <PackageVersion Include="WolverineFx" Version="6.25.1" />
    <PackageVersion Include="WolverineFx.Http" Version="6.25.1" />
    <PackageVersion Include="WolverineFx.Marten" Version="6.25.1" />
    <!-- مُصَرِّف Roslyn وَقت التَشغيل: Wolverine 6 لَم يَعُد يَشحَنه في الـ core -->
    <PackageVersion Include="WolverineFx.RuntimeCompilation" Version="6.25.1" />

    <!-- ASP.NET runtime bits -->
    <PackageVersion Include="Microsoft.AspNetCore.Components.Web" Version="10.0.10" />
    <PackageVersion Include="Microsoft.AspNetCore.SignalR.Client" Version="10.0.10" />

    <!-- Web Push (PWA notifications) -->
    <PackageVersion Include="WebPush" Version="1.0.13" />

    <!-- ‏(رُفِعَ يَومَ ‏2026-08-30 — ‏ADR-018: ‏`StackExchange.Redis` و
         `Microsoft.AspNetCore.SignalR.StackExchangeRedis`. كانَتا
         تَخدُمانِ مَشروعَينِ بِصِفرِ إحالَةٍ مِن أَيّ `csproj` مَشحون،
         ولا خادِمَ Redis في النَشرِ أَصلاً. حُذِفَ المَشروعانِ
         فَسَقَطَتِ الحُزمَتان مَعَهُما.) -->

    <!-- تَخزينُ كائِناتٍ مُتَوافِقٌ مَع S3 (‏Cloudflare R2) — العَميلُ
         الرَسميُّ يَحمِل تَوقيعَ SigV4، فَلا يُكتَب بِاليَد. -->
    <PackageVersion Include="AWSSDK.S3" Version="4.0.102.4" />

    <!-- Excel export (feasibility study) -->
    <PackageVersion Include="ClosedXML" Version="0.105.1" />

    <!-- JSON Schema validation (بَوّابَة مُصادَقَة أَدَوات الوَكيل) -->
    <PackageVersion Include="JsonSchema.Net" Version="9.4.0" />

    <!-- SMTP (قَناة OTP بَريديَّة — Auth.Providers.Smtp) -->
    <PackageVersion Include="MailKit" Version="4.17.0" />
    <PackageVersion Include="MimeKit" Version="4.17.0" />

    <!-- Tests -->
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.8.1" />
    <PackageVersion Include="xunit" Version="2.9.3" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.1.5" />

    <!-- Logging -->
    <PackageVersion Include="Serilog.AspNetCore" Version="10.0.0" />
    <PackageVersion Include="Serilog.Sinks.Console" Version="6.1.1" />

    <!-- Importer tool -->
    <PackageVersion Include="Microsoft.Extensions.Hosting"            Version="10.0.10" />
    <PackageVersion Include="Microsoft.Extensions.Configuration.Json" Version="10.0.10" />
    <PackageVersion Include="Microsoft.Data.SqlClient"                Version="7.0.2" />
    <PackageVersion Include="Dapper"                                  Version="2.1.79" />
    <PackageVersion Include="System.CommandLine"                      Version="2.0.10" />
  </ItemGroup>
</Project>
