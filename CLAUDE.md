# CLAUDE.md — منصّة وسايل

## نَظرَة عامّة
منصّة تجارة وخدمات **متعدّدة المستأجرين، عربيّة أوّلًا**، على **.NET 10** و**Blazor SSR** و**Marten** (مصدر أحداث + وثائق على Postgres، بإيجار مقترن) و**Wolverine** (وسيط + نقاط HTTP ساكنة). إدارة حزم مركزيّة (`Directory.Packages.props`)، والحلّ `PlatformV1.slnx`.

**الأطروحة الحاكمة**: نواة صلبة صغيرة + طبقة تعريفات بيانات (JSON) + وكلاء يؤلّفون التعريفات خلف مصادِق وموافقة بشريّة. «مطبخ ذكيّ ووصفات، لا روبوت طبّاخ».

**ابدأ من `docs/README.md`** — فهرس التوثيق. ولا تفترض معلومة معماريّة دون التحقّق منها هناك أوّلًا، **ثم في الكود**؛ فإن اختلفا **فالكود هو الحقيقة** والوثيقة دَين يُصحَّح.

---

## قواعد صارمة — لكلٍّ كلفتها المقيسة

> كل قاعدة أدناه كُتبت بعد كلفة وقعت فعلًا في هذا المستودع أو سلفه، إلّا ما وُسم **«وقائيّة»**. والكلفة جزء من القاعدة: من يعرف الثمن يلتزم.

### ١. التجريد لا يسبق مستهلكه
لا تُنشئ واجهة ولا مكتبة ولا طبقة **قبل أن يوجد من يستهلكها في وقت التشغيل**. والقاعدة العمليّة: **ثلاثة مستهلكين قبل الاستخراج**، ويُكتب شرط الاستخراج داخل الملف نفسه.

**الكلفة — ستّ حالات مقيسة**: ‏527 سطر تجريد تراسل (`INotificationChannel` وعائلتها) لا يبلغها razor ولا js ولا `Program.cs`، صُمِّمت لعقد Wolverine outbox **لم يُفعَّل قطّ** (`grep PublishAsync` = صفر) — والمنتج الحيّ يتجاوزها بخمس نسخ يدويّة؛ ونسخة `widgets.css` في مشروع `Widgets` **كود ميّت يُبنى ويُشحن ولا يُقرأ** (91 سطر تباعُد)؛ ودفتر قيود كامل في المستودع القديم بصفر مستهلكين وثلاثة أعطال صامتة؛ وخمسة مشاريع مزوّدين لا يشير إليها أيّ `csproj`؛ و`QuotaConsumed`/`QuotaRefunded` أحداث بمنطق `Apply` كامل **وبلا باعث واحد**؛ و`ACommerce.Platform.Flows` نفسها بقيت مرجَّعة من مشروع الاختبار وحده حتى وُصلت عمدًا.

**الفحص الآليّ**: كل حدث له `Apply` يجب أن يكون له مُصدِر واحد على الأقل، بقائمة يتامى مثبَّتة تتقلّص — فلا تنمو إلّا بقرار مرئيّ في مراجعة.

### ٢. الحدّ الذي لا يُقاس آليًّا يَنهار
اسم الحدّ لا يحفظه: لا `interface`، ولا `.csproj`، ولا وثيقة، ولا نمط معلَن. **يحفظه اختبار يفشل عند خرقه.**

**الكلفة — أربع حالات**: واجهة ماتت؛ ومشروع مفصول انجرف بصمت؛ ووثيقتان (`KIT-PATTERN.md`, `PITFALLS.md`) تصفان بنية ومسارات **لا وجود لها**؛ ونمط البوّابات المعلَن (واجهات العلامة) مستعمَل في **موضع واحد من نحو خمسين**.

**المشتقّ**: ألزِم ما يُفحص آليًّا، وأبقِ ما سواه إرشادًا. ولا تكتب قاعدة لا تستطيع كتابة اختبارها.

### ٣. التوصيف يخضرّ قبل التبديل، ولا يُمسّ بعده
قبل نقل أي سلوك من كود إلى بيانات (أو من موضع إلى آخر): اكتب اختبارًا يثبّت السلوك القائم، أخضِره، **أودِعه في كوميت مستقلّ**، ثم بدّل — والاختبار يبقى أخضر **بلا تعديل حرف**. وبرهانه بصمة الملف قبل وبعد.

**الكلفة**: هذه هي القاعدة التي جعلت خمس موجات متتالية (الأدوار، التصيير، الثيم، التدفّقات، أدوار وقت التشغيل) تمرّ **بصفر انحدار** — والمرّات التي قِيس فيها التطابق بايتًا ببايت كشفت ما لا تراه العين.

### ٤. السياسة بيانات: معجم مغلق + مصادِق برموز خرق ثابتة
كلّ تنوّع يُعبَّر عنه **باختيار من مجموعة منتهية** يصير بيانات: ملف تعريف + مصادِق + وثيقة مستأجر + دورة اعتماد + كاش بمفتاح المستأجر + تكافؤ صفريّ. وكلّ تنوّع يتطلّب **حسابًا على حالة وقت التشغيل** يبقى كودًا خلف مفتاح.

**هذا هو المعيار الفاصل، ويُطبَّق قبل الكتابة لا بعدها.** مقيس: الأدوار والثيم عبَرا كاملَين؛ و«إلى مَن» في المراسلة وطبقة الأثر قاومتا لأنهما مُسنَدان لا قيم.

شكل المصادِق موحّد: `record XViolation(string Code, string MessageAr)` + `Validate(T) : IReadOnlyList<XViolation>` + `IsValid`. ولكلّ رمز خرق **اختبار موجب واختبار سالب**.

### ٥. الوصف يصير بيانات، والأثر يبقى كودًا
التدفّق يُوصَف بيانات: حالات، انتقالات، فاعل مخوّل، حالات نهائيّة **معلَنة لا مشتقّة**. أمّا **أثر الانتقال** فمفتاح من معجم مغلق يُحيل إلى كود — ولا يُوصَف في الملف.

**الكلفة — تجربة مضادّة مقيسة في المستودع القديم**: وُحِّد الأثر بين المال والرسائل، فبُنيت رسالة المحادثة بالباني المحاسبيّ نفسه (`Entry.Create("message.send").From(…,1).To(…,1)`) بمحلّل توازن يعمل. النتيجة: انحطّت المحاسبة إلى طقس شكليّ (استعلام قراءة يُصاغ «قيدًا متوازنًا» بقيمة 1)، **ولم ينل المال ما يحتاجه** (لا عملة على الطرف، لا حساب، ورصيد يرجع صفرًا دائمًا). آثار الرسائل تبتلع أخطاءها عمدًا وتقع في معاملة منفصلة وبلا منع تكرار — **مقبول لرسالة، قاتل لريال**. توحيد الأثر يجعل المال يرث عادات الرسائل لا العكس.

### ٦. الحراسة في التوقيع لا في الجسم
كل نقطة تُعدِّل حالة **تعلن حارسها** في توقيعها أو بمرشّح، لا بسطر داخل الجسم. **والتخويل يسبق تحقّق الحقول** — وإلّا صار خطأ التحقّق قناعًا للثغرة.

**الكلفة**: ‏40 صفحة إدارة مستأجر كانت تُرسم كاملةً لطلب مجهول (فيها أرقام هواتف وهويّات نفاذ)؛ و`POST /admin/tenants/create` بلا بوّابة يرتدّ `302` عن حقل ناقص **فيبدو محروسًا وهو مكشوف** — وبجسم صحيح كان يُنشئ مستأجرًا. والجذر واحد: الحراسة كانت سطرًا في الجسم فلم يُرَ نسيانها.

### ٧. مُعترضٌ يقرأ حالةً مخزَّنة ومُعترضٌ يحوّل فقط لا يتشاركان أنبوبًا
المحور ليس «يرفض أم يحوّل» بل **«هل يقرأ حالة مخزَّنة؟»** — والصلاحية نتيجة له. **وقابل للفحص بتوقيع الدالّة**: من يقبل `IDocumentSession` ومن لا يقبلها لا يجتمعان.

**الكلفة**: ‏`Results.Forbid()` كان يرمي **500** لأن خدمة المصادقة غير مسجَّلة — أي أن **عطبًا في آلية التحويل صار عطبًا في مسار الرفض**؛ وفي المستودع القديم مُهايئ معترضات يمرّر `null` دائمًا فلا يُحفظ منع التكرار قطّ ويرجع الرصيد صفرًا.

### ٨. لا أنبوب رابع: استعمل القائم أو أصلحه
قبل بناء آلية عابرة للطبقات، **ابحث هل يفعلها المستودع أصلًا**. أمرُ `grep` واحد أرخص من موجة.

**الكلفة المقيسة**: عندنا أربعة أنابيب اعتراض مبنيّة ومهجورة — `RequirePermission` (مستهلك واحد من 99 نقطة)، و`GatePipeline` (98 سطرًا، مستهلك واحد)، وWolverine middleware (صفر)، وMarten listeners (صفر). فالمشكلة ليست غياب أنبوب بل **أنابيب بلا مستعمِل**.

### ٩. الهيكل العظميّ الماشي قبل التوثيق الشامل
ممنوع توثيق تفصيليّ أو تنفيذ ميزة كاملة قبل أن تمرّ **نسخة مصغّرة حيّة** عبر كل الطبقات وتعمل فعلًا. فريق فرديّ، والتوثيق النظريّ قبل التحقّق العمليّ مخاطرة توقّف.

### ١٠. القياس هو الحكم — بشرط أن تكون الأداة نفسها مقيسة
لا تُقدِّر بالعين ما يمكن عدّه. وإذا اختلفت العين والأداة فالأداة هي الحكم — **ما لم تكن الأداة نفسها غير مقيسة**.

**الكلفة**: أداة التحقّق البصريّ أعطت «صفر مخالفة» وهي **عمياء عن الفيضان**: مرّ العطب دون أن يُرى **ثلاث مرّات** (قواعد احتواء تفحص أزواجًا مسمّاة؛ و`scrollWidth` الابن يساوي `clientWidth`ه؛ وسلف قاصّ يخفي الفيضان عن مستوى المستند) — ولم يُكشف إلّا بحقن عيب مصطنع. **فكل أداة تحقّق تطبع عدد ما فحصته، وتفشل إن كان صفرًا**؛ و«صفر مخالفة» بلا عدّاد لا يُميَّز عن أداة عمياء.
وفي الجلسة نفسها قارن وكيلٌ مستودعًا **بنفسه** بسبب خطأ في أمر، فأعطى «صفر فروق» — فأُعيد القياس. **الأداة تُقاس قبل أن يُوثَق بها.**

### ١١. كلّ نصّ يراه المستخدم من قاموس مفاتيح، لا حرفيًّا في الكود — **وقائيّة**
الآلية **متعدّدة اللغات لا لغتين**: خريطة مفتوحة بمفاتيح لغات، لا حقلَي `Ar`/`En`. والعربيّة إلزاميّة بالمصادقة. والنصوص القائمة تُرحَّل إلى القاموس **بالعربيّة وحدها** أوّلًا، **وكل دفعة ترحيل تُبرهن بمقارنة بايتيّة أن المخرَج المُصيَّر لم يتغيّر** (فالتشكيل يسقط صامتًا وإلّا).

**الحساب**: الاصطلاح **مجّانيّ** على شاشة تُكتب اليوم، وكلفته على شاشة مكتوبة هي فتحها وتعديلها وإعادة اختبارها. القياس عند التسجيل: ‏2,696 سطرًا يحمل محارف عربيّة في **89 ملف Razor من 146**. المرور واقع لا محالة؛ والسؤال: أنمرّ على 89 اليوم أم على 150 بعد أشهر؟

**والاتّجاه تخطيط لا ترجمة**: يُكتب التخطيط بخصائص منطقيّة (`margin-inline-start`, `inset-inline-*`, `text-align: start`) فيصير الاتّجاه مجّانيًّا؛ ولا تُكتب قاعدة `[dir=rtl]` إلّا لما يستحيل التعبير عنه منطقيًّا. المرجع المقيس: القديم فيه **ثلاث** قواعد اتّجاه في 3,800 سطر CSS. ودَيننا اليوم: **15 قاعدة فيزيائيّة مقابل 81 منطقيّة** — تُسدَّد قبل إظهار زرّ اللغة يومًا.

### ١٢. الميزة لا تكون مُنجَزة حتى تُبلَغ بالنقر
شاشة مبنيّة لا يصل إليها المستخدم **غير موجودة**. **ومدخلٌ يجيب بـ«قريبًا» ليس مدخلًا** — فإمّا أن يفتح أو يُخفى حتى يفتح.

**والأسوأ من «قريبًا» مدخلٌ يضرّ**: زرّ اللغة كان يقلب الاتّجاه إلى LTR على واجهة عربيّة بالكامل **ولا يترجم حرفًا** (مؤشّر `L["…"]` بصفر موضع استدعاء) — فالنقرة لا تُفضي إلى لا شيء، بل إلى تخطيط مكسور.
**وميزةٌ تُباع ولا تُفرَض أسوأ**: `AllowCustomPattern` تُعرض في بطاقة الأسعار ونافذة الترقية **وصفر موضع يفحصها**.

### ١٣. أثبت أن التغيير محصور حيث قصدته
عند تبديل أصل ثنائيّ أو قيمة عابرة (رمز ثيم، ورقة أنماط): **قِس** — استخرج السابق، قارن بعتبة صارمة، واطبع المربّع المحيط بالفروق. «لم يتغيّر شيء مهمّ» دعوى بلا برهان.

### ١٤. غياب ارتباط الأداة ليس غياب القدرة
«الأداة غير مسجَّلة» و«لا سبيل إلى هذه القدرة» طبقتان مختلفتان. افحص القدرة بنداء حقيقيّ قبل إعلان التعذّر، وإن تعذّرت فبلّغ **بالأداة الناقصة تحديدًا** لا بالنتيجة.

**الكلفة المقلوبة (ربح)**: ‏Node وPlaywright غير مثبَّتين (مقيس: مسح قرصين بعمق أربعة) — والقدرة موجودة: Chrome يتكلّم **CDP** و.NET فيها `ClientWebSocket`. فبُنيت الطبقة السادسة بلا Node، ومحرّكها هو محرّك Playwright نفسه.

### ١٥. أيّ قرار معماريّ جديد أو مخالفة لقرار قائم تتطلّب ADR
في `docs/16_Decisions/` أو ما يقابله، **قبل أو مع** التغيير. لا انحراف معماريّ صامت.

### ١٦. لا تخترع بيانات منتج ولا توازن اقتصاديّ
أسماء الباقات والحدود والعملات والأدوار تُؤخذ من التوثيق أو تُسأل. الرقم غير الموجود في `docs/` يحتاج توضيحًا لا افتراضًا.

---

## بيئة التطوير

```bash
export DOTNET_ROOT=/d/Testor/dotnet10 && export PATH="/d/Testor/dotnet10:$PATH"

# الخادم — اقتل ما على المنفذ قبل البناء، وأعده بعده
netstat -ano | grep ":5050 .*LISTENING"        # ← taskkill //F //PID <pid>
CS="$(tr -d '\r\n' < /d/Testor/neon-connection.txt)"; CS="${CS%\"}"; CS="${CS#\"}"
export ConnectionStrings__Postgres="$CS"
export ASPNETCORE_ENVIRONMENT=Development
dotnet run --no-launch-profile --project apps/V1.App --urls=http://localhost:5050
```

- **ممنوع طباعة محتوى ملف الاتصال** بأي حال. اقرأه إلى متغيّر بيئة ولا تعرضه.
- **قاعدة الإنتاج لا يُشغَّل عليها التطوير**: ‏Marten 9 يعيد كتابة مخطّط Marten 8 (‏`DROP FUNCTION cascade`) بمجرّد الإقلاع، والبذّارات تكتب بيانات. اعمل على **فرع Neon** منسوخ، وتحقّق من المضيف قبل كل إقلاع.
- **صلاحية مشرف المنصّة لا تُمنح تلقائيًّا**: ‏`PLATFORM_ADMIN_PHONE` + (بيئة Development أو `PLATFORM_ADMIN_BOOTSTRAP=1`).
- **التحقّق البصريّ**: `dotnet run scripts/verify-runtime.cs -- [--viewport 390x844] <url>` — ويُقاس بنسبة الجهاز لا بنافذة المكتب.
- **مستأجرون**: `ashare` **عرض المستثمرين — لا تُعدَّل بياناته، ولا تُفتح صفحات تفصيل إعلاناته** (الفتح يكتب حدث مشاهدة). للتجارب: `theme-demo`, `owner-test`.

## سير العمل

- **كوميت لكل بند مكتمل**، لا تجميع ولا تأجيل. صيغة: `feat(scope): …`, `fix(scope): …`, `test(scope): …`, `docs(scope): …`, `refactor(scope): …`, `chore(scope): …`. والوصف عربيّ يذكر **السبب لا العَرَض**.
- **في الجولات المستقلّة: كوميتات محليّة فقط، والدفع دفعة واحدة بحضور المالك.**
- الاختبارات **لا تنقص أبدًا**. أرضيتها تُذكر في كل تكليف.
- **لا تلمس** `docs/Wasayel_Package-*` (غرفة صفقة المستثمرين) — وهي متجاهَلة في `.gitignore`.

## عند الشكّ
لا تخمّن. **قِس**. وإن تعذّر القياس فاسأل، ووثّق السؤال في التقرير بدل افتراض قرار.
