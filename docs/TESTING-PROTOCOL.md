# بروتوكول اختبارات الحتمية وجودة القوالب

> **الغرض**: تعريف بوابة الجودة التي لا يمر توليد آلي (وكيل أو قالب) إلى
> الإنتاج إلا عبرها. البروتوكول يقيس شيئين منفصلين: **حتمية المولِّد**
> (كتلة أ) و**جودة الأثر المولَّد** (كتلة ب).
>
> **الوضع الحالي**: الخطوات 1 و2 و4 من §5 منفذة (2026-08-09) — مشروع
> `tests/ACommerce.Platform.Tests` + بوابة CI + بوابة مصادقة المخطط في
> المنفذ + `DealsPolicy` كبيانات مع مصادقات T5/T6 (بلا تخزين لكل مستأجر
> بعد). هذه الوثيقة تعرّف *ماذا* يُقاس قبل كتابة أي جهاز، امتداداً
> لمنهجية [heritage/VERIFICATION-LAYERS.md](heritage/VERIFICATION-LAYERS.md).

## 1. تعريف الحتمية — بأمانة

مزودو النماذج لا يضمنون تطابقاً بتّياً حتى بحرارة صفر. لذلك يفرّق
البروتوكول بين نوعين من الضمان:

- **ضمان بنيوي (100% إلزامي)**: ما يُفرض بالمخطط والتعداد والمنفّذ —
  خرقه عيب في السياج لا في النموذج، وأي فشل فيه يوقف الدمج.
- **قياس إحصائي (عتبات معلنة)**: اتساق المخرجات عبر التشغيلات — يُعرَّف
  كصفوف تكافؤ دلالي (JSON مُقنّن ومقارن حقلاً بحقل)، لا كمطابقة نصية.

## 2. المقاييس

| الرمز | التعريف | العتبة |
|---|---|---|
| **V₁** | نسبة التوليدات الصالحة (مخططاً ودلالةً) من أول محاولة | ≥ 85% |
| **V₃** | نسبة الصلاحية خلال ≤ 3 دورات إصلاح ذاتي | ≥ 95% |
| **CR** | معدل الاتساق: نفس الطلب + نفس الحالة × k تشغيلات ← نفس صف التكافؤ | ≥ 90% (k=5) |
| **CV** | معدل خرق الكاتالوج/المخطط الواصل إلى التنفيذ | **= 0 دائماً** |
| **C** | التغطية: نسبة طلبات التهيئة الحقيقية القابلة للتعبير بالأدوات/الأنماط الحالية | يُقاس ويُتتبع (بوابة توسيع اللغة، لا بوابة دمج) |

## 3. كتلة أ — حتمية المولِّد (تعمل على الوكيل الحالي فوراً)

### T1 — توافق المخطط
ذخيرة طلبات عربية حقيقية (من جلسات الاستوديو المحفوظة) تُعاد على الوكيل؛
كل `InputJson` يُصادَق ضد مخطط أداته. يقيس **V₁** و**V₃**.

### T2 — الاستنساخية
نفس الطلب + نفس لقطة الحالة × 5 تشغيلات؛ مقارنة الحمولات بعد التقنين
(ترتيب مفاتيح، إزالة حقول غير جوهرية). يقيس **CR**، ويكشف حساسية الموجّه
لأي تغيير في البرومبت أو النموذج.

### T3 — أمان الكاتالوج المغلق *(الاختبار البنيوي)*
طلبات عدائية تحاول: اختراع دور خارج التعداد، نوع خاصية غير معتمد، slug
مخالف للنمط، أيقونة تتجاوز 256 ك.ب. **الشرط المسبق**: بوابة مصادقة
JSON-Schema في `AgentToolExecutor` (الفجوة الموثقة في
[AGENT-TOOLS §5](AGENT-TOOLS.md)) — **نُفذت 2026-08-09**
(`AgentToolValidator`) وبذرة هذا الاختبار في
`tests/ACommerce.Platform.Tests/AgentToolValidatorTests.cs` — بعدها يصبح
**CV = 0** ضماناً مُبرهناً، وهذا الاختبار حارسه الدائم.

### T4 — الجلسات الذهبية
كل جلسة اعتُمدت بشرياً (`applied`) تنضم لذخيرة انحدار؛ أي تعديل على
البرومبت أو ترقية نموذج تُقاس ضد الذخيرة قبل النشر. الرفضات (`rejected`)
أمثلة سالبة يجب ألا يعيد الوكيل إنتاجها.

## 4. كتلة ب — جودة القالب (تُفعَّل مع رفع `DealsPolicy` إلى بيانات)

### T5 — الصحة البنيوية للنمط
كل تعريف نمط مولَّد: مراحله من مفردات `DealStage` الثماني حصراً، يبدأ بـ
`Offered`، ينتهي بمرحلة نهائية، لكل مرحلة فاعل من الأربعة، ولا تكرار.

**نُفذت 2026-08-09**: `DealPatternValidator.ValidateStructure` — دالة نقية
فوق `DealPatternDefinition`، ترجع قائمة خروق برموز ثابتة
(`stages_empty`, `stage_out_of_vocabulary`, `first_stage_not_offered`,
`duplicate_stage`, `actor_missing`, `actor_out_of_vocabulary`,
`label_missing`, `pattern_name_empty`). الاختبارات في
`tests/ACommerce.Platform.Tests/DealPatternValidatorTests.cs` تثبت
الاتجاهين: الأنماط الخمسة تجتاز، وأنماط فاسدة مصطنعة تُرفض بالرمز المقصود.

### T6 — الخصائص المُبرهنة
الآلة خطية وصغيرة، فالفحص الاستنفادي شبه مجاني: حتمية الانتهاء، لا مرحلة
يتيمة لا يملك أحد تحريكها، مساران معرّفان من كل مرحلة (إلغاء + نزاع)،
والاكتمال التلقائي عند آخر مرحلة يعمل.

**نُفذ منها 2026-08-09**: `DealPatternValidator.ValidateProperties` —
حتمية الانتهاء (المشي بـ `Next` من أول مرحلة ينتهي عند آخر مرحلة معلنة
بلا زيارة مكررة: `termination_not_deterministic`, `walk_misses_last_stage`)
ولا مرحلة يتيمة (`orphan_stage_unreachable`, `orphan_stage_unmovable`).
**والباقي متروك لـ T7 قصداً**: مسارا الإلغاء والنزاع والاكتمال التلقائي
سلوك `DealsService` لا سلوك التعريف، ولا يُفحصان بدالة نقية على بيانات.
وكذلك **لم** يُجعل «الانتهاء بـ `Reviewed`» شرطاً — `classifieds` ينتهي
عند `Confirmed` قصداً، وجعله خرقاً يرفض نمطاً قياسياً قائماً.

### صنف الأثر الثاني — تعريفات الأدوار *(نُفذ 2026-08-10)*
كتلة ب لم تعد نمط الصفقة وحده. `RoleDefinition` (كاتالوج الأدوار كملفات
JSON، META-MODEL §6) **أثر مولَّد بنفس المعنى**: بيانات تصف سلوكاً،
يكتبها وكيل أو بشر، وتدخل النظام. ولذلك تمر من نفس البوابة:
`RoleDefinitionValidator` — دوال نقية، رموز خرق ثابتة، والاتجاهان
مختبَران (السبعة تجتاز، وأدوار فاسدة مصطنعة تُرفض بالرمز المقصود). وهي
**مفروضة عند التحميل** لا متاحة: تعريف فاسد يُفشل الإقلاع باسم الدور
ورمز الخرق. الفرق عن T5/T6: لا خصائص مُبرهنة هنا (لا آلة حالات تُمشى)،
بل صحة بنيوية ومعاجم مغلقة — الصلاحيات والمكونات وأنواع الحقول.

#### برهان HTML للتحويلات التي تمسّ التصيير *(2026-08-10)*
حين يمسّ التحويل **التصيير** لا المنطق وحده، اختبار الوحدة يثبّت القرار
ولا يثبّت المُخرَج. أُضيفت لذلك خطوة ثانية رخيصة: التقاط صفحات مرجعية
بـ `curl` من خادم حي **قبل** التعديل وبعده، ومقارنتها بايتاً ببايت.

- **الشرط الذي يجعلها برهاناً لا طقساً**: يُلتقط الأساس **مرتين من نفس
  الكود** أولاً، ويُطبَّع الفرق بينهما — فما بقي مختلفاً بعد التطبيع هو
  وحده ما يحمل معنى. هنا كان المتغيّر الوحيد تعليق
  `<!--Blazor-Server-Component-State:…-->` (حمولة معمّاة تتغير كل طلب)،
  وبعد تطبيعه صار الالتقاطان **متطابقين بايتاً ببايت** — وعندها فقط صحّت
  المقارنة قبل/بعد.
- **حدّ معلن**: التغطية بحدود أدوار المستأجر المُفعَّلة فعلاً. في
  `ashare` ثلاثة (`customer`/`vendor`/`host`)، فمسارات `riderHome`
  و`driverHome` لم يبلغها الالتقاط الحيّ — ويغطيها التوصيف على الأدوار
  السبعة كلها. الطبقتان تكملان ولا تُغني إحداهما عن الأخرى.

### T7 — القبول بالمحاكاة
تشغيل النمط المولَّد عبر `DealsService` الحقيقي بفاعلين مبرمجين
(initiator/counterparty/platform bots): المسار السعيد كاملاً، ثم إلغاء
عند **كل** مرحلة، ثم نزاع عند كل مرحلة. التحقق: سلامة الخط الزمني
(StageBefore/After متسقان، الفاعل مخوّل)، والحالة النهائية صحيحة.
البذرة الموجودة (زر seed للصفقات التجريبية) هي نواة هذا الجهاز.

### T8 — تكامل العرض
القالب يُصيّر فعلاً: الـ stepper يعرض كل المراحل، `LabelAr` معرّفة لكل
مرحلة مستخدمة، شخصية النمط (`PatternProfile`) مشتقة بلا تعارض، وPWA
الدور صالحة التثبيت.

## 5. ترتيب التنفيذ ومعايير القتل

| الخطوة | تفعّل | معيار التوقف/القتل |
|---|---|---|
| 1. مشروع xUnit + CI gate — **نُفذت 2026-08-09** (`tests/ACommerce.Platform.Tests` + ‏`.github/workflows/ci.yml`) | البنية | لا دمج بعدها إلا باختبارات خضراء |
| 2. بوابة مصادقة المخطط في المنفذ — **نُفذت 2026-08-09** (`AgentToolValidator` أول بوابة في `ExecuteAsync`) | T3 (وCV=0) | فشل واحد بعد البوابة = عيب سياج، أولوية قصوى |
| 3. جهاز T1/T2/T4 على الوكيل الحالي | كتلة أ | V₃ < 90% على اللغة الضيقة الحالية ⇒ لا توسيع للأدوات قبل إصلاح الموجهات |
| 4. رفع `DealsPolicy` إلى بيانات — **نُفذت 2026-08-09** (`DealPatternDefinition` + `DealPatternCatalog` + `DealPatternValidator`، بحدّ معلن: **بلا تخزين بعد**) | T5/T6 | خلف نفس `DealsService`؛ سلوك الأنماط الخمسة الحالية **لا يتغير بتاً** (اختبار توصيف قبل/بعد) — استُوفي: `DealsPolicyCharacterizationTests` كُتب واخضرّ قبل الرفع في كوميت مستقل ولم يُمس سطر منه بعده |
| 5. جهاز T7–T8 (T5/T6 قائمان) | كتلة ب | أي نمط مولَّد يفشل T5/T6 يُرفض آلياً قبل وصوله للمراجعة البشرية |
| 6. الأدوار ملفات — **نُفذت 2026-08-10** (`RoleDefinition` + `Definitions/*.role.json` + `RoleDefinitionValidator` + `PermissionCatalog`، بحدّ معلن: **التصيير لا يقرأ `composition` بعد**) | كتلة ب (صنف الأثر الثاني) | خلف نفس `RoleCatalog`؛ سلوك القوالب السبعة **لا يتغير بتاً** (اختبار توصيف قبل/بعد) — استُوفي: `RoleCatalogCharacterizationTests` كُتب واخضرّ قبل النقل في كوميت مستقل ولم يُمس سطر منه بعده |
| 7. التصيير يقرأ التركيب — **نُفذت 2026-08-10** (`RoleCompositionResolver` + قاموس مغلق في كل موضع + `dealPatternAffinity`؛ الحدّ في البند ٦ **رُفع**) | كتلة ب (صنف الأثر الثاني) | المواضع الستة + `PatternFromTenant` **لا يتغير سلوكها بتاً** — استُوفي بـ **برهان ثلاثي**: `RoleCompositionCharacterizationTests` كُتب واخضرّ على الـ `switch` في كوميت مستقل قبل التبديل ولم يُمس بعده، ثم اخضرّ على القراءة من الملفات؛ و**عشرون صفحة مرجعية** ملتقطة بـ curl من خادم حي قبل/بعد **متطابقة بايتاً ببايت** |

**قاعدة القرار الكبرى**: إن حققت الكتلتان عتباتهما على الأنماط الخمسة
القياسية + عشرة أنماط مولَّدة تجريبية، فمتجر القوالب جاهز تقنياً للفتح.
وإن عجزت V₃ عن عتبتها إلا بنموذج أكبر وأغلى، يُعاد تسعير التوليد قبل
أي التزام تجاري — القرار بالأرقام لا بالحدس.

---
*صيغ هذا البروتوكول على أساس مسح الكود في 2026-08-09 عند الكوميت
`23067e3e`، وهو المرجع التنفيذي لبند «الاختبارات» الغائب من
[PRODUCTION-PLAN](PRODUCTION-PLAN.md).*
