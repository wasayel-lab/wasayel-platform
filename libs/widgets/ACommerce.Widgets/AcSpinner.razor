@namespace ACommerce.Widgets

<span class="ac-spinner @(Large ? "ac-spinner-lg" : "") @(Class ?? "")"></span>

@code {
    [Parameter] public bool Large { get; set; }
    [Parameter] public string? Class { get; set; }
}
