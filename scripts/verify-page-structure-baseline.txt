# scripts/verify-page-structure-baseline.txt — PINNED DEBT for Layer 1
#
# Format: <rule>|<file>|<count>   — measured 2026-08-11 at commit d08f0adc.
# The line number is deliberately NOT part of the fingerprint: edits above a
# violation would churn the ledger without changing the debt.  Adding or
# removing a violation in an already-listed file still moves the count and
# still trips the gate.
#
# Bidirectional, like Layer 2 and the FlowInventoryTests dead-state ledger:
# a new entry fails the gate, and a retired entry that is still pinned fails
# it too.  Nothing here was repaired by the agent that measured it.
#
# ── no-inline-color (34 -> 26) ───────────────────────────────────────────
# Literal #hex in a style="" attribute.  These bypass the --wsl-* -> --ac-*
# token layer entirely (META-MODEL §9), so a tenant theme can never restyle
# them: status pills stay mint-green and amber whatever the tenant picks.
# Concentrated in the Studio/incubator screens, which were built fastest.
#
# 2026-08-15: EIGHT retired, and only the eight whose literal equalled an
# existing token EXACTLY and whose token is the semantically right one:
#   color:#fff over background:var(--ac-primary)  -> var(--ac-on-primary)
#     ChatRoom, Plans, VendorProfile — the house pattern, used 14 times in
#     the stylesheets already.
#   color:#fff over an arbitrary dark fill        -> var(--ac-text-on-dark)
#     TenantEdit (the tenant's own BrandColor), StudioShell (--st-primary).
# Both tokens are literal #ffffff, defined once in widgets.css, and are NOT
# redefined by any tenant branding sheet nor by body.ac-dark — measured, so
# the substitution is provably zero-change today AND stays zero.
#
# The rest were NOT touched, and the reason is written down rather than
# implied: their literals equal no token (mint #dcfce7, amber #fef3c7, the
# rating gold #f59e0b…).  Tinting a star rating with --ac-warning would be
# zero-change today and a bug the first time a tenant paints warning red.
# They are measured and costed in docs/THEME-DEBT-DECISIONS.md.
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/Pages/Admin/QualityMonitor.razor|2
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/Pages/StudioApp.razor|7
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/Pages/StudioAppConsole.razor|5
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/Pages/StudioAppTicketDetail.razor|1
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/Pages/StudioHome.razor|1
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/Pages/StudioStudy.razor|2
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/Pages/StudioStudyPrint.razor|3
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/Pages/VendorProfile.razor|2
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/RoleHomes/RiderHome.razor|1
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/StudioShell.razor|1
no-inline-color|libs/templates/ACommerce.Templates.Customer.Marketplace/Components/UpgradePrompt.razor|1

# ── no-raw-bootstrap-button (1) ──────────────────────────────────────────
# `<button class="btn btn-primary">` in the SHARED error state — the one
# widget every failure path renders.  Should be AcButton.
no-raw-bootstrap-button|libs/templates/ACommerce.Templates.Shared/AcErrorState.razor|1

# ── unlinked-rcl-css (0) — RETIRED 2026-08-15 ────────────────────────────
# Was the heaviest finding of this layer: two referenced libraries shipped a
# stylesheet App.razor never linked, because the Marketplace template kept
# its OWN copy of each under wwwroot/css/ and linked that instead.
#
#   libs/widgets/ACommerce.Widgets/wwwroot/widgets.css            (1933 lines, DEAD)
#     vs .../Marketplace/wwwroot/css/widgets.css                  (1920 lines, SERVED)
#     -> measured divergence at deletion: 52 lines dead-only, 39 live-only.
#   libs/templates/ACommerce.Templates.Shared/wwwroot/templates-shared.css (DEAD)
#     vs .../Marketplace/wwwroot/css/templates-shared.css                  (SERVED)
#     -> 1 line of divergence (max(6px,…) vs the served max(8px,…)).
#
# RESOLVED by DELETING both dead copies (commit "الحَدّ المَكسور"), not by
# re-pointing the link: re-pointing would have changed one <link href> in
# every rendered page and broken the byte-for-byte appearance gate, and it
# would have forced an edit to ThemeZeroEquivalenceTests' frozen path — the
# characterization test that must stay green without a character changed.
# The served copy was measurably the NEWER one (it carries the --wsl-* token
# wiring and the density calc); the dead ones were pre-token snapshots.
#
# One thing was lost with the dead widgets.css and is NOT re-added here,
# because re-adding it is a VISIBLE change on /ashare/explore (the
# zero-equivalence yardstick): the four .ac-space-trust* rules (26 lines).
# See docs/THEME-DEBT-DECISIONS.md item D-1 for the measurement and the
# `git show` incantation that restores them.
